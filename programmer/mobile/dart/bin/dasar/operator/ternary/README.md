```dart
import '../../saya.dart';

void main() {
  print(pembatas * 5);

  var nilai = 50;
  var ucapan = nilai >= 75
      ? 'Operator berhasil di jalankan'
      : nilai; // $ variabel dikembalikan sebagai default
  print('$hijau$ucapan\n$rege');

  var value = 10;
  var variabel = value >= 80 ? 'Nilai 80' : 'Nilai 70'; // $ penggunaan seperti if-else
  print('$kuning$variabel$rege');

  print(pembatas * 5);
}
```
