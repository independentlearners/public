Jalankan perintah berikut di dalam direktori Lua

```pwsh
clear && lua -i -l lua -e "_PROMPT='\n      >>>>>   '"
```

Perintah `dofile("file.lua")` digunakan untuk mengeksekusi file `.lua` gunakan perintah ini didalam **REPL**
